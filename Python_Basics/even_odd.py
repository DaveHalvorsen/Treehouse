# This function accepts a # and checks if it's even
# The check is done with modulo for 2. If modulo 2
# is equivalent to 0 (evenly divisilbe by 2.
def even_odd(number):
    if number % 2 == 0:
        return True
    else:
        return False
